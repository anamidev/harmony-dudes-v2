export default function NotFound() {
    return (
        <section className="flex min-h-[calc(100vh-209px)] items-center justify-center">
            Sorry, this page is not found :(
        </section>
    );
}
